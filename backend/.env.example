PORT=5000
# Atlas → Connect → Drivers → choose "Standard connection string" (not SRV) if querySrv fails on Windows
MONGO_URI=mongodb://<username>:<password>@host1:27017,host2:27017,host3:27017/?ssl=true&replicaSet=...&authSource=admin
JWT_SECRET=your_jwt_secret_here
